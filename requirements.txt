# Required Python packages for Ultimate Enigma Messenger

ttkbootstrap>=1.10.0
cryptography>=41.0.0
argon2-cffi>=23.1.0
qrcode[pil]>=7.4   # QR code generation for TOTP setup
pytest>=7.0.0      # optional, only needed for running tests